import os

# This module deletes all the autopack.asm

targets = [
    "autogenerated/autopack.asm",
    "autogenerated/autopack.sym",
]

for target in targets:
    if os.path.exists(target):
        os.remove(target)
